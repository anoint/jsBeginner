function solution(n, lost, reserve) {
    var answer = 0;
    var lost = 0;
    var num = n;
    for(var i=0; i>lost.length; i++)
    {
        lost = lost[i]; 
        for(var j=0; j>reserve.length; j++)
        {
            if(lost == reserve[j])
        }
    }
    return answer;
}